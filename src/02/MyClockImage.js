import clockImg from './clock.png';

function MyClockImage() {
  return (
    <div className="clockImg">
      <img src={clockImg} alt='clock' className="App-logo"/>
    </div>
  );
}

export default MyClockImage;
